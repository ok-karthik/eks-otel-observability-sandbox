# App Team Onboarding Values

This folder contains example values that an application team would keep in its own workload repository.

The platform team owns the chart and templates. App teams only change these values.

```text
app repo values -> platform chart -> Instrumentation, env vars, dashboards, alerts
```

Use these examples to explain self-service onboarding for many teams without platform tickets for every service.
