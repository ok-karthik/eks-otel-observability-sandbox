serviceName: "product-info-service"
serviceNamespace: "catalog"
serviceVersion: "1.0.0"
environment: "dev"
ownerTeam: "catalog-platform"
tenantId: "catalog"
language: "python"
slackChannel: "#alerts-catalog"

instrumentation:
  mode: "operator"
  annotation: "instrumentation.opentelemetry.io/inject-python"
  instrumentationRef: "python-instrumentation"

resourceAttributes:
  service.name: "product-info-service"
  service.namespace: "catalog"
  service.version: "1.0.0"
  deployment.environment: "dev"
  team: "catalog-platform"
  tenant.id: "catalog"

dashboards:
  enabled: true
  template: "python-service"

alerts:
  enabled: true
  latencyP99Ms: 750
  errorRatePercentage: 5
